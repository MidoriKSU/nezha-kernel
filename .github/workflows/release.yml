name: Release · Publish Artifacts

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string
      kernel_ver:
        required: true
        type: string
      release_title:
        required: true
        type: string

jobs:
  release:
    name: Publish · ${{ inputs.kernel_ver }}
    runs-on: ubuntu-latest
    steps:
      - name: Dummy
        run: echo "ok"
